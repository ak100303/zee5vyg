name: Schedule Test (Every 10 Minutes)

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: |
          echo "Hello! This workflow ran at:"
          date
